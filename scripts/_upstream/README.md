Upstream TabICL curriculum scripts (archived for reference).

- `regression_curriculum.sh`, `train_stage1.sh`, `train_stage2.sh`,
  `train_stage3.sh`: original TabICL author's regression curriculum.
- `survival_curriculum.sh`, `train_survival_stage1_auto.sh`: previous
  survival pre-generation and HPC scripts, replaced by the canonical
  on-the-fly `train_survival_curriculum.sh` at the parent level.
